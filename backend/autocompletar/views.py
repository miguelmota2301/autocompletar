from django.shortcuts import render
from django.http import HttpResponse, JsonResponse
import json

"""with open('/home/miguel/jusbrasil/autocompletarv3/backend/autocompletar/data.json', 'r') as f:
    data = json.load(f)

def index(request):
    #return HttpResponse('Olá')
    if query:
        sugestoes = []
        for item in data:
            if query.lower() in item["Series_Title"].lower():
                sugestoes.append(item["Series_Title"])
    return JsonResponse(data, safe=False)"""

# Create your views here.
